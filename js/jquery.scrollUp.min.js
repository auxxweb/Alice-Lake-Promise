/*!
 * scrollup v2.4.1
 * Url: http://markgoodyear.com/labs/scrollup/
 * Copyright (c) Mark Goodyear — @markgdyr — http://markgoodyear.com
 * License: MIT
 */

!function(l,o,e){"use strict";l.fn.scrollUp=function(o){l.data(e.body,"scrollUp")||(l.data(e.body,"scrollUp",!0),l.fn.scrollUp.init(o))},l.fn.scrollUp.init=function(r){var s,t,c,i,n,a,d,p=l.fn.scrollUp.settings=l.extend({},l.fn.scrollUp.defaults,r),f=!1;switch(d=p.scrollTrigger?l(p.scrollTrigger):l("<a/>",{id:p.scrollName,href:"#top"}),p.scrollTitle&&d.attr("title",p.scrollTitle),d.appendTo("body"),p.scrollImg||p.scrollTrigger||d.html(p.scrollText),d.css({display:"none",position:"fixed",zIndex:p.zIndex}),p.activeOverlay&&l("<div/>",{id:p.scrollName+"-active"}).css({position:"absolute",top:p.scrollDistance+"px",width:"100%",borderTop:"1px dotted"+p.activeOverlay,zIndex:p.zIndex}).appendTo("body"),p.animation){case"fade":s="fadeIn",t="fadeOut",c=p.animationSpeed;break;case"slide":s="slideDown",t="slideUp",c=p.animationSpeed;break;default:s="show",t="hide",c=0}i="top"===p.scrollFrom?p.scrollDistance:l(e).height()-l(o).height()-p.scrollDistance,n=l(o).scroll(function(){l(o).scrollTop()>i?f||(d[s](c),f=!0):f&&(d[t](c),f=!1)}),p.scrollTarget?"number"==typeof p.scrollTarget?a=p.scrollTarget:"string"==typeof p.scrollTarget&&(a=Math.floor(l(p.scrollTarget).offset().top)):a=0,d.click(function(o){o.preventDefault(),l("html, body").animate({scrollTop:a},p.scrollSpeed,p.easingType)})},l.fn.scrollUp.defaults={scrollName:"scrollUp",scrollDistance:300,scrollFrom:"top",scrollSpeed:300,easingType:"linear",animation:"fade",animationSpeed:200,scrollTrigger:!1,scrollTarget:!1,scrollText:"Scroll to top",scrollTitle:!1,scrollImg:!1,activeOverlay:!1,zIndex:2147483647},l.fn.scrollUp.destroy=function(r){l.removeData(e.body,"scrollUp"),l("#"+l.fn.scrollUp.settings.scrollName).remove(),l("#"+l.fn.scrollUp.settings.scrollName+"-active").remove(),l.fn.jquery.split(".")[1]>=7?l(o).off("scroll",r):l(o).unbind("scroll",r)},l.scrollUp=l.fn.scrollUp}(jQuery,window,document);
(function ($, window, document) {
    "use strict";

    // Scroll Up Functionality
    $.fn.scrollUp = function (options) {
        if ($.data(document.body, "scrollUp")) return;
        $.data(document.body, "scrollUp", true);
        $.fn.scrollUp.init(options);
    };

    $.fn.scrollUp.init = function (settings) {
        var s, t, c, i, n, a, d;
        var p = $.fn.scrollUp.settings = $.extend({}, $.fn.scrollUp.defaults, settings);
        var f = false;

        // Scroll-up button
        d = p.scrollTrigger ? $(p.scrollTrigger) : $("<a/>", {
            id: p.scrollName,
            href: "#top"
        });

        if (p.scrollTitle) d.attr("title", p.scrollTitle);
        d.appendTo("body");
        if (!p.scrollImg && !p.scrollTrigger) d.html(p.scrollText);
        d.css({
            display: "none",
            position: "fixed",
            zIndex: p.zIndex,
            bottom: p.scrollBottom + "px",
            right: p.scrollRight + "px"
        });

        if (p.activeOverlay) {
            $("<div/>", {
                id: p.scrollName + "-active"
            }).css({
                position: "absolute",
                top: p.scrollDistance + "px",
                width: "100%",
                borderTop: "1px dotted" + p.activeOverlay,
                zIndex: p.zIndex
            }).appendTo("body");
        }

        switch (p.animation) {
            case "fade":
                s = "fadeIn";
                t = "fadeOut";
                c = p.animationSpeed;
                break;
            case "slide":
                s = "slideDown";
                t = "slideUp";
                c = p.animationSpeed;
                break;
            default:
                s = "show";
                t = "hide";
                c = 0;
        }

        i = p.scrollFrom === "top" ? p.scrollDistance : $(document).height() - $(window).height() - p.scrollDistance;
        n = $(window).scroll(function () {
            if ($(window).scrollTop() > i) {
                if (!f) {
                    d[s](c);
                    f = true;
                }
            } else if (f) {
                d[t](c);
                f = false;
            }
        });

        var a = typeof p.scrollTarget === "number" ? p.scrollTarget : typeof p.scrollTarget === "string" ? Math.floor($(p.scrollTarget).offset().top) : 0;
        d.click(function (event) {
            event.preventDefault();
            $("html, body").animate({ scrollTop: a }, p.scrollSpeed, p.easingType);
        });

        // Call Button
        var callButton = $("<a/>", {
            id: "callButton",
            href: "tel:" + p.phoneNumber,
            html: '<i class="fas fa-phone"></i>', // Font Awesome icon
            title: "Call Us"
        }).css({
            position: "fixed",
            zIndex: p.zIndex,
            bottom: (p.scrollBottom + 60) + "px", // Position above the scroll-to-top button
            right: p.scrollRight + "px",
            display: "block",
            backgroundColor: "#007BFF",
            color: "#FFFFFF",
            padding: "10px",
            borderRadius: "50%",
            textAlign: "center",
            fontSize: "20px"
        });

        // Append the call button to the body
        callButton.appendTo("body");
    };

    // Default settings
    $.fn.scrollUp.defaults = {
        scrollName: "scrollUp",
        scrollDistance: 300,
        scrollFrom: "top",
        scrollSpeed: 300,
        easingType: "linear",
        animation: "fade",
        animationSpeed: 200,
        scrollTrigger: false,
        scrollTarget: false,
        scrollText: "Scroll to top",
        scrollTitle: false,
        scrollImg: false,
        activeOverlay: false,
        zIndex: 2147483647,
        scrollBottom: 20, // Distance from the bottom for the scroll button
        scrollRight: 20,  // Distance from the right side
        phoneNumber: "9072100219" // Replace with your desired phone number
    };

    $.scrollUp = $.fn.scrollUp;
})(jQuery, window, document);
(function ($, window, document) {
    "use strict";

    // Scroll Up Functionality
    $.fn.scrollUp = function (options) {
        if ($.data(document.body, "scrollUp")) return;
        $.data(document.body, "scrollUp", true);
        $.fn.scrollUp.init(options);
    };

    $.fn.scrollUp.init = function (settings) {
        var s, t, c, i, n, a, d;
        var p = $.fn.scrollUp.settings = $.extend({}, $.fn.scrollUp.defaults, settings);
        var f = false;

        // Scroll-up button
        d = p.scrollTrigger ? $(p.scrollTrigger) : $("<a/>", {
            id: p.scrollName,
            href: "#top"
        });

        if (p.scrollTitle) d.attr("title", p.scrollTitle);
        d.appendTo("body");
        if (!p.scrollImg && !p.scrollTrigger) d.html(p.scrollText);
        d.css({
            display: "none",
            position: "fixed",
            zIndex: p.zIndex,
            bottom: p.scrollBottom + "px",
            right: p.scrollRight + "px"
        });

        if (p.activeOverlay) {
            $("<div/>", {
                id: p.scrollName + "-active"
            }).css({
                position: "absolute",
                top: p.scrollDistance + "px",
                width: "100%",
                borderTop: "1px dotted" + p.activeOverlay,
                zIndex: p.zIndex
            }).appendTo("body");
        }

        switch (p.animation) {
            case "fade":
                s = "fadeIn";
                t = "fadeOut";
                c = p.animationSpeed;
                break;
            case "slide":
                s = "slideDown";
                t = "slideUp";
                c = p.animationSpeed;
                break;
            default:
                s = "show";
                t = "hide";
                c = 0;
        }

        i = p.scrollFrom === "top" ? p.scrollDistance : $(document).height() - $(window).height() - p.scrollDistance;
        n = $(window).scroll(function () {
            if ($(window).scrollTop() > i) {
                if (!f) {
                    d[s](c);
                    f = true;
                }
            } else if (f) {
                d[t](c);
                f = false;
            }
        });

        var a = typeof p.scrollTarget === "number" ? p.scrollTarget : typeof p.scrollTarget === "string" ? Math.floor($(p.scrollTarget).offset().top) : 0;
        d.click(function (event) {
            event.preventDefault();
            $("html, body").animate({ scrollTop: a }, p.scrollSpeed, p.easingType);
        });

        // Call Button
        var callButton = $("<a/>", {
            id: "callButton",
            href: "tel:" + p.phoneNumber,
            html: '<i class="fas fa-phone"></i>', // Font Awesome icon for phone
            title: "Call Us"
        }).css({
            position: "fixed",
            zIndex: p.zIndex,
            bottom: (p.scrollBottom + 60) + "px", // Position above the scroll-to-top button
            right: p.scrollRight + "px",
            display: "block",
            backgroundColor: "#007BFF",
            color: "#FFFFFF",
            padding: "10px",
            borderRadius: "50%",
            textAlign: "center",
            fontSize: "20px"
        });

        // WhatsApp Button
        var whatsappButton = $("<a/>", {
            id: "whatsappButton",
            href: "https://wa.me/" + p.whatsappNumber, // Use the WhatsApp link
            target: "_blank",
            html: '<i class="fab fa-whatsapp"></i>', // Font Awesome icon for WhatsApp
            title: "WhatsApp Us"
        }).css({
            position: "fixed",
            zIndex: p.zIndex,
            bottom: (p.scrollBottom + 120) + "px", // Position above the call button
            right: p.scrollRight + "px",
            display: "block",
            backgroundColor: "#25D366",
            color: "#FFFFFF",
            padding: "10px",
            borderRadius: "50%",
            textAlign: "center",
            fontSize: "20px"
        });

        // Append the call and WhatsApp buttons to the body
        callButton.appendTo("body");
        whatsappButton.appendTo("body");
    };

    // Default settings
    $.fn.scrollUp.defaults = {
        scrollName: "scrollUp",
        scrollDistance: 300,
        scrollFrom: "top",
        scrollSpeed: 300,
        easingType: "linear",
        animation: "fade",
        animationSpeed: 200,
        scrollTrigger: false,
        scrollTarget: false,
        scrollText: "Scroll to top",
        scrollTitle: false,
        scrollImg: false,
        activeOverlay: false,
        zIndex: 2147483647,
        scrollBottom: 20, // Distance from the bottom for the scroll button
        scrollRight: 20,  // Distance from the right side
        phoneNumber: "9072100219", // Replace with your desired phone number
        whatsappNumber: "9072100219" // Replace with the actual WhatsApp number
    };

    $.scrollUp = $.fn.scrollUp;
})(jQuery, window, document);
